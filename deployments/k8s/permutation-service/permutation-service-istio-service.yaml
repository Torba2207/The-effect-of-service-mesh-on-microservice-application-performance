apiVersion: v1
kind: Service
metadata:
  name: permutation-service
  labels:
    app: permutation-service
spec:
  type: ClusterIP  # Changed from NodePort so Istio can manage external access
  selector:
    app: permutation-service
  ports:
    - name: http
      port: 80
      targetPort: 5001
      protocol: TCP