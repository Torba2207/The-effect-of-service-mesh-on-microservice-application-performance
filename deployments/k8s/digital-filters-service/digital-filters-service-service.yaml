apiVersion: v1
kind: Service
metadata:
  name: digital-filters-service
  labels:
    app: digital-filters-service
spec:
  type: NodePort
  selector:
    app: digital-filters-service
  ports:
    - name: http
      port: 80
      targetPort: 5007
      nodePort: 30007
      protocol: TCP
